# Server Configuration
PORT=3000

# Worker Configuration
MAX_CONCURRENT_WORKERS=200
WORKER_BATCH_SIZE=200
RATE_LIMIT_DELAY=250
MAX_DEPTH=2
SUBPAGE_CONCURRENCY=4
PLAYWRIGHT_MAX_CONTEXTS=200

# Scrape Delay (jitter applied before each request; set to 0 to disable)
SCRAPE_DELAY_MIN_MS=100
SCRAPE_DELAY_MAX_MS=400

# Supabase Configuration (Optional - for persistent storage)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Example Supabase values (replace with your actual values):
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1Ni...............
